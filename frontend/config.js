// Base URL of the FastAPI backend (see backend/app.py). No trailing slash.
// Change this if your backend runs somewhere other than localhost:8000.
window.TEXTGUARD_API_BASE_URL = "http://127.0.0.1:2200";
